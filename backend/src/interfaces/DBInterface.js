const Connection = require('../interfaces/Connection');
const Model = require('../interfaces/Model');
const Schema = require('../interfaces/Schema');

module.exports = {
    Connection,
    Model,
    Schema
};